fn main() {
    // sample data from cyberpunk 2077
    let weapon_types = ["Assault Rifle", "Sniper Rifle", "Shotgun", "Pistol", "SMG"];

    // count the number of weapon types
    let number_weapon_types = weapon_types.len();

    // display the result
    println!("Number of weapon types: {}", number_weapon_types);
}